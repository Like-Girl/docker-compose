version: '2'

services:
  apollo-db:
    image: mysql:5.7
    container_name: mysql
    environment:
      TZ: Asia/Shanghai
    #  MYSQL_ALLOW_EMPTY_PASSWORD: 'yes'
      MYSQL_ROOT_PASSWORD: '123456'
    ports:
      - "3306:3306"
